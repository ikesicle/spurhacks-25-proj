import * as React from 'react';

const Title = ({name}) => {
    return(
        <h1>Hello {name}</h1>
    );
}

export { Title };

const Body = () => {
    return(
        <h1></h1>
    );
}

export { Body };

